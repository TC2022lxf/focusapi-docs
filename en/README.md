# English documentation (placeholder)

English pages are reserved for a future release. Files mirror the Chinese structure under the repository root.

When ready to publish:

1. Translate each `*.mdx` into this directory (same paths).
2. Add a `languages` array to `docs.json` per [Mintlify i18n](https://mintlify.com/docs/guides/internationalization).
3. Set `default: true` on `zh-Hans` (or keep Chinese at root and map `en/*` paths).

Until then, these files are **not** linked in `docs.json` navigation.
